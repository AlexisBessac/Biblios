<form method="post" action="{{ path('app_admin_author_delete', {'id': author.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cet auteur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button class="btn btn-danger" title="Supprimer">Supprimer</button>
</form>

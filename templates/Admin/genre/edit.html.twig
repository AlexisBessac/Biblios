{% extends 'base.html.twig' %}

{% block title %}Modifier un genre{% endblock %}

{% block body %}
<main>
    <section>
        <div class="container my-5">
            <h1 class="mb-4 text-center">Modifier le genre</h1>
    
            <div class="my-4">
                <a href="{{ path('app_admin_genre_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
            </div>
    
            <div class="card p-4 shadow-sm">
                {{ include('admin/genre/_form.html.twig', {'button_label': 'Modifier', 'button_title': 'Modifier'}) }}
            </div>
        </div>
    </section>
</main>
{% endblock %}

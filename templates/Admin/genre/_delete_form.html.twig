<form method="post" action="{{ path('app_admin_genre_delete', {'id': genre.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce genre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genre.id) }}">
    <button class="btn btn-outline-danger" title="Supprimer">Supprimer</button>
</form>

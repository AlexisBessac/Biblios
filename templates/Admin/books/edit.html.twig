{% extends 'base.html.twig' %}

{% block title %}Modifier un Livre{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="mb-4 text-center">Modifier le livre</h1>

        <div class="my-4">
            <a href="{{ path('app_admin_books_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>

        <div class="card p-4 shadow-sm">
            {{ include('admin/books/_form.html.twig', {'button_label': 'Modifier', 'button_title': 'Modifier'}) }}
        </div>
    </div>
{% endblock %}
